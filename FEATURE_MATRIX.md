# Feature Matrix

Hackathon: Reddit Mod Tools and Migrated Apps Hackathon
Idea: Moderator Decision Memory
Readiness pass: 2026-05-22 IST

| Feature / claim | Source evidence | Implementation path | Test / proof | Status | Gap / blocker |
|---|---|---|---|---|---|
| Responsive moderator command dashboard | Build plan demo path and UI template plan first-screen judge moment | `src/client/main.tsx`, `src/client/styles.css`, `index.html` | `npm run build`; `npm run e2e:local` screenshots at `outputs/e2e/local/screenshots/home-375.png`, `home-768.png`, `home-1440.png`; no horizontal overflow at all three widths | proven-local | Formal M2 `/polish` unavailable due SSH timeout to `m2worker`. |
| Fixture queue item selection with matched precedents | Build plan demo steps 1-3 | `fixtureQueueItems`, `fixtureDecisions`, `matchPrecedents`, React queue buttons and precedent panel | `npm test` 7/7; `npm run e2e:local` selected "Heated reply in market crash thread" and verified the active item changed | proven-local | Queue content is fixture demo data until installed in Devvit. |
| Manual local decision capture | Build plan demo steps 4-5 | React form calls `createDecisionRecord` and stores `mdm-decisions-v1` in `localStorage` | `npm run e2e:local` verified local memory grew from 5 to 6; screenshot `outputs/e2e/local/screenshots/flow-after-save-1440.png` | proven-local-fixture | Local browser path is not Redis; UI/README/report label fixture/localStorage behavior. |
| Local failure state for invalid capture | Readiness no-dummy-action contract | `captureDecision` catches validation errors and renders `.form-feedback` with `aria-live` | `npm run e2e:local` cleared summary and waited for `summary must be 1-320 characters` | proven-local | None for local failure path. |
| Copy explanation template action | Primary moderator workflow | `copyTemplate` uses Clipboard API and reports success/fallback | `npm run e2e:local` clicked first "Copy template" and waited for success/fallback status | proven-local | Clipboard can be unavailable in some browsers; UI now gives explicit fallback. |
| Shared validation, matching, and retention logic | Required app scope: validation, transparent matching, retention metadata | `src/shared/memory.ts`, `src/shared/types.ts`, `src/shared/fixtures.ts` | `npm test`: shared tests cover legal precedent ranking, decision creation/expiry, and expired cleanup | proven | None for local logic. |
| Server API health and demo state | README Devvit integration notes | `src/server/index.ts` Hono app | `npm run e2e:api`: `/api/health` 200 and `/api/demo-state` 200 with `fixture-empty` | proven-local | Redis-backed decisions require Devvit runtime. |
| Server API decision write and match contracts | API plan and README endpoint list | `POST /api/decisions`, `POST /api/match`, `saveDecision`, `listDecisions` | `src/server/index.test.ts` mocked Redis contract test stores a decision and returns transparent matches; `npm run e2e:api` verifies invalid 400 and Devvit-runtime-required 503 | contract-tested | Live Redis write blocked until Devvit login/upload/playtest succeeds. |
| Devvit moderator menu/form capture | Build plan real integration choice | `devvit.json` menu/forms plus `/internal/menu/capture-decision` and `/internal/form/capture-decision-submit` | Typecheck/build pass; server tests cover adjacent API contract and scheduler endpoint | contract-tested-static | Not live playtested because Devvit CLI is not logged in and OAuth self-service is network-blocked. |
| Devvit Redis persistence and rule index | Build plan real write path | `redis.hSet` into `decisions:index` and `rule:<tag>:decisions` | Mocked Redis test proves expected contract shape; `npm run e2e:api` proves local valid write honestly returns 503 outside Devvit runtime | contract-tested-blocked-live | Real Redis persistence unproven until Devvit runtime is initialized. |
| Scheduler retention cleanup | Build plan and `devvit.json` scheduler task | `/internal/scheduler/retention-cleanup`; `/internal/menu/schedule-retention-cleanup` | Shared cleanup unit test; mocked scheduler endpoint test schedules `retention-cleanup` | contract-tested | Live scheduler execution unproven without Devvit upload/playtest. |
| Optional `onModAction` ingestion | Build plan: optional after payload spike | `devvit.json` trigger and `/internal/triggers/mod-action` | Typecheck/build pass | implemented-not-live-proven | Optional path only; no live Reddit payload proof. Do not claim as primary demo dependency. |
| Platform auth preflight | Readiness auth contract | `scripts/devvit-auth-preflight.mjs`, `npm run e2e:auth` | `npx devvit --version` passed; `npx devvit whoami` returned `Not currently logged in`; output saved to `outputs/e2e/devvit-auth-preflight.json` | auth-blocked | Self-service OAuth opened in browser but Reddit network security blocked it. |
| Public GitHub repo | Builder report repo claim | `origin` remote and GitHub repo | `gh repo view ...` returned PUBLIC repo; `git ls-remote origin main` returned `9039fae...` | proven | Current hardening/readiness edits are local and not pushed. |
| Devpost submission readiness | Browser execution runbook and prior hardening portal check | Read-only Devpost browser check from previous hardening | Prior screenshot showed Gabriel account menu plus "Join hackathon" gate | blocked | Joining/registering is approval-gated; no draft created and no final submit attempted. |
